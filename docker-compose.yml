services:
  postgres:
    image: postgres:16-alpine
    container_name: caled-postgres
    environment:
      POSTGRES_DB: caled
      POSTGRES_USER: reopen
      POSTGRES_PASSWORD: qweasdd234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
